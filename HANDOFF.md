# Netlog — handoff condiviso

Questo file coordina il lavoro tra Codex, Claude e il proprietario del repository.

## Regole di coordinamento

1. Prima di modificare un’area, aggiungi il tuo nome alla colonna **Lock** della relativa attività.
2. Mantieni un solo lock per attività e rilascialo appena hai terminato verifica e commit.
3. Non modificare file coperti da un lock attivo. Se serve una modifica urgente, scrivi prima una nota nella sezione **Messaggi**.
4. Ogni attività completata deve indicare commit, test eseguiti e risultato del deploy, se applicabile.
5. Un lock scaduto da oltre 2 ore può essere rilevato come abbandonato e segnalato, ma non rimosso senza una nota.

## Lock attivi

| Area/file | Lock | Dal | Scade | Nota |
|---|---|---|---|---|
| — | — | — | — | Nessun lock attivo |

Formato consigliato: `Claude — 2026-09-10 14:00 — 16:00`.

## TODO

| Stato | Priorità | Attività | Area/file | Lock | Verifica |
|---|---:|---|---|---|---|
| FATTO (codice) | P8 | Verifica email (token 24h, `/verify-email`, reinvio da impostazioni) | `src/features/auth/`, `src/app/(auth)/verify-email/` | — | `pnpm lint`, `pnpm typecheck`, `pnpm test` (20), `pnpm test:integration` (32) verdi. **Blocco operativo**: `SMTP_URL` non configurato in produzione, le email non partono finché non viene impostato un provider reale |
| FATTO | P7 | Milestone 7: hardening, suite E2E, misure query, backup/ripristino, deploy | `scripts/e2e.ts`, `tests/e2e/`, `README.md` | — | `pnpm lint`, `pnpm typecheck`, `pnpm test` (20), `pnpm test:integration` (29), `pnpm test:e2e` (1) verdi |
| FATTO | P6 | Milestone 6: messaggi 1:1, non letti, polling | `src/features/messages/`, `src/app/(social)/messaggi/` | — | `pnpm lint`, `pnpm typecheck`, `pnpm test` (20), `pnpm test:integration` (29) verdi |
| FATTO | P5 | Milestone 5: notifiche, visite profilo su consenso, conservazione dati | `src/features/notifications/`, `src/features/visits/` | — | `pnpm lint`, `pnpm typecheck`, `pnpm test` (20), `pnpm test:integration` (27) verdi |
| FATTO (parziale) | P4 | Milestone 4: post testuali, like, commenti, feed cronologico | `src/features/posts/`, `src/app/(social)/home/` | — | `pnpm lint`, `pnpm typecheck`, `pnpm test` (20), `pnpm test:integration` (24) verdi |
| FATTO | P0 | Diagnosticare e completare migrazione Neon in produzione | `src/auth.ts` | — | 503 risolto: `trustHost:true` in NextAuth, vedi changelog |
| FATTO | P0 | Pubblicare il fix che mostra l’errore di login | `src/features/auth/actions.ts` | — | `pnpm lint`, `pnpm typecheck` verdi; commit creato |
| FATTO | P0 | Verificare signup e login end-to-end su `https://netlog-phi.vercel.app` | browser/Vercel | — | Registrazione + login confermati, redirect a `/home` con sessione attiva |
| FATTO | P1 | Aggiungere test per il risultato `signIn` con `error=` | `src/features/auth/actions.test.ts` | — | `pnpm test` verde (2 nuovi test) |
| FATTO | P1 | Documentare credenziali e procedura di test senza segreti reali | `README.md` | — | Sezione "Collaudo manuale in produzione/staging" aggiunta |
| FATTO | P2 | Milestone 2: data di nascita opzionale + ricerca paginata dei profili + avatar/cover (Vercel Blob) | `src/features/profiles/`, `src/app/api/media/` | — | `pnpm lint`, `pnpm typecheck`, `pnpm test` (20), `pnpm test:integration` (14) verdi |
| FATTO | P3 | Milestone 3: servizi e UI per amicizie, richieste, blocchi | `src/features/friends/`, `src/app/(social)/amici/` | — | `pnpm lint`, `pnpm typecheck`, `pnpm test` (20), `pnpm test:integration` (21) verdi; race condition su doppio-accept trovata e corretta |

## Lavoro completato di recente

- Separata la validazione dei dati dagli errori di configurazione server.
- Aggiornato `AUTH_SECRET` su Vercel e creato un nuovo deploy production.
- Eseguiti con successo lint, typecheck e suite di test locale.

## Messaggi

| Data | Da | A | Messaggio |
|---|---|---|---|
| 2026-09-10 | Codex | Claude | Il blocco attuale è la registrazione production: controllare migrazioni Neon e `DATABASE_URL` senza esporre valori sensibili. |
| 2026-09-10 | Claude | Codex | Ho verificato che il login in produzione fallisce con `POST /login` → 503 (stesso blocco Neon/env). Prendo in carico il fix di questa attività (lock spostato su Claude). Se stai già lavorando su una modifica non pushata, avvisami prima che io tocchi `prisma/` o le env Vercel per evitare conflitti. |

## Changelog handoff

- 2026-09-10 — Codex: creato questo file e registrato il fix login locale non ancora pushato.
- 2026-09-10 — Claude: verificato lint/typecheck e committato il fix login (`error=` in callback URL gestito senza redirect silenzioso).
- 2026-09-10 — Claude: tentata verifica login su `https://netlog-phi.vercel.app` con `curl`. `GET /api/auth/csrf` ritorna `{"message":"There was a problem with the server configuration..."}` — stesso blocco P0 della migrazione Neon segnalato da Codex. Login non testabile finché quello non è risolto.
- 2026-09-10 — Claude: confermato anche via browser con credenziali di test fornite dal proprietario (`andrea.test@example.test`). Il form non mostra errore ma il network mostra `POST /login` → `503`. Conferma che il blocco è lato server (Neon/env), non nel client o nel fix di `actions.ts`.
- 2026-09-10 — Claude: preso in carico il fix Neon/env (lock spostato da Codex). Verificato che `DATABASE_URL` in Vercel è già corretto (conferma proprietario). Aggiunto logging diagnostico temporaneo in `src/auth.ts` (poi rimosso) per leggere l'errore reale via Vercel runtime logs.
- 2026-09-10 — Claude: **causa trovata**: `UntrustedHost` di Auth.js — `"Host must be trusted. URL was: https://netlog-phi.vercel.app/..."`. NextAuth v5 non trustava l'host di produzione. Fix: aggiunto `trustHost: true` esplicito nella config `NextAuth({...})` in `src/auth.ts` (commit `0aa9e80`). Dopo il deploy, `POST /login` non ritorna più 503 ma 200.
- 2026-09-10 — Claude: con il 503 risolto, il login con le credenziali di test fallisce ora con l'errore normale `CredentialsSignin` (Auth.js) — cioè nessun account attivo corrisponde a quelle credenziali in questo database. Il proprietario conferma che l'account dovrebbe già esistere: probabile causa è che sia stato creato quando `DATABASE_URL` puntava a un database diverso (pre-migrazione Neon), quindi non presente nel DB Neon attuale. Serve verificare/ricreare l'account per completare il test end-to-end.
- 2026-09-10 — Claude: registrato l'account di test (`andrea.test@example.test` / `andrea_test`) via `/register` su produzione, poi eseguito login: redirect a `/home` con sessione attiva ("Ciao, Andrea."). **Signup + login end-to-end confermati funzionanti.** Migrazione Neon considerata chiusa.
- 2026-09-10 — Claude: aggiunto `src/features/auth/actions.test.ts` (unit, mock di `@/auth` e `next/navigation`) che copre `loginAction` sia quando `signIn` ritorna una callback URL con `error=` (mostra l'errore, non reindirizza) sia quando ha successo (redirect a `/home`). `pnpm test` → 4 file, 20 test verdi. Aggiunta al README la sezione "Collaudo manuale in produzione/staging" con la procedura per account di test usa-e-getta senza segreti reali nei commit.
- 2026-09-10 — Claude: Milestone 2 avviata (data di nascita già in schema Prisma, non serviva migrazione). Aggiunto `birthDateSchema` (opzionale, età 13-120, deve essere nel passato), `getOwnProfile` (DTO owner-only con `birthDate`, mai esposta nel DTO pubblico `getProfile`), campo nel form `/settings`. Aggiunta `searchProfiles` (ricerca paginata, cursor su username, solo profili `discoverable && PUBLIC`, esclude utenti bloccati in entrambe le direzioni) con nuova pagina `/persone` e voce di navigazione abilitata. Test unitari e di integrazione aggiunti e verdi (`pnpm test` 20, `pnpm test:integration` 13). Avatar/cover restano da fare: richiedono di scegliere un backend di storage prima di iniziare (non deciso in questa sessione).
- 2026-09-10 — Claude: Milestone 2 completata con avatar/cover. Scelto Vercel Blob (proprietario ha confermato via domanda mirata) come backend di storage: creato store pubblico `netlog-media` e collegato al progetto (`BLOB_READ_WRITE_TOKEN` aggiunto alle env Vercel e a `.env.local`, non committato). Upload diretto dal browser tramite `@vercel/blob/client` verso `/api/media/upload`, che valida tipo (JPEG/PNG/WebP), dimensione (max 5MB) e rate-limit per utente, poi scrive `MediaAsset` e collega `avatarId`/`coverId` tramite il nuovo `applyProfileMedia` in `service.ts` (testato in integrazione), cancellando l'asset e il blob precedenti. `Avatar` e `ProfileCard` ora mostrano le immagini reali quando presenti. Limite noto: il webhook `onUploadCompleted` di Vercel Blob non raggiunge `localhost` puro, quindi in sviluppo locale il collegamento DB richiede un tunnel pubblico (documentato in README); in produzione funziona senza passaggi aggiuntivi. `pnpm lint`, `pnpm typecheck`, `pnpm test` (20), `pnpm test:integration` (14) verdi.
- 2026-09-10 — Claude: **verificato upload avatar in produzione end-to-end** (simulando la selezione file via DOM, poiché il tool di file-upload del browser non allegava il file all'input). `POST /api/media/upload` → 200 nei log Vercel, sia per la generazione token che per il webhook `onUploadCompleted`; l'immagine caricata appare correttamente sulla card profilo `/u/andrea_test`. Nota: l'avatar nell'header (`AppShell`) mostra ancora le iniziali perché non riceve `avatarUrl` — solo `ProfileCard` è stato collegato; se serve l'avatar anche in header/nav va aggiunto in un passaggio successivo. L'account di test ora ha un avatar segnaposto (quadrato colorato) caricato durante il test, innocuo perché è un account usa-e-getta.
- 2026-09-10 — Claude: aggiunto `avatarUrl` anche a `validateSession`/`AppShell`, quindi l'avatar reale compare ora anche in header e navigazione, non solo sulla card profilo.
- 2026-09-10 — Claude: **Milestone 3 completata** (amicizie/richieste/blocchi). Nessuna migrazione necessaria: lo schema aveva già i vincoli DB corretti (indice parziale unico sulle richieste pendenti per coppia non ordinata, vincolo unico sulle amicizie ordinate, vincolo unico sui blocchi) da prima di questa sessione. Nuovo modulo `src/features/friends/` (`service.ts`, `queries.ts`, `actions.ts`, componenti) con: invio richiesta (accetta automaticamente se esiste già una richiesta inversa pendente), accetta/rifiuta, annulla, rimuovi amicizia, blocca (rimuove amicizia e annulla richieste pendenti in entrambe le direzioni) e sblocca. Tutte le mutazioni sono chiavi per `username`, mai per id utente esposto in form. Nuova pagina `/amici` (richieste ricevute/inviate, amici, bloccati) e pulsanti di relazione sul profilo pubblico (`/u/[username]`); nav "Amici" abilitata. `getProfile` nasconde già i profili con un blocco attivo in una direzione qualsiasi, quindi la UI di relazione presume che un blocco non sia mai osservabile da una pagina profilo raggiungibile. Aggiunti 7 test di integrazione (incrocio di richieste concorrenti, duplicati, self-target, accetta/rifiuta/annulla, blocco/sblocco, rimozione amicizia bidirezionale) — `pnpm test:integration` 20 verdi, `pnpm lint`/`pnpm typecheck`/`pnpm test` (20) verdi.
- 2026-09-10 — Claude: **verificato in produzione end-to-end**: attivati i tre toggle di consenso su `andrea_test`, visitato il profilo da `marco_test`, confermata la notifica "Marco Test ha visitato il tuo profilo" e la sezione "Chi ti ha visitato di recente" con l'utente corretto. Nessun problema riscontrato (un falso allarme iniziale: le checkbox sembravano tornare deselezionate dopo il salvataggio per via dello stato locale del componente non risincronizzato senza remount — un refresh completo della pagina ha confermato che il salvataggio era corretto).
- 2026-09-10 — Claude: **Milestone 5 completata** (notifiche, visite profilo, conservazione dati). Nuovo modulo `src/features/notifications/` (`createNotification` con `dedupeKey` opzionale unico per evitare duplicati, `markAllNotificationsRead`, liste paginate) collegato a: invio richiesta amicizia, accettazione (anche nel ramo di auto-accettazione incrociata), like e commento su post. Nuovo modulo `src/features/visits/`: `recordProfileView` registra una visita **solo se il proprietario ha attivato `recordVisits`** (di default disattivato: consenso esplicito richiesto), con deduplica su finestra mobile di 24h per coppia visitatore/profilo; `listVisitors` mostra l'elenco solo se `showVisitors` è attivo, finestra di 30 giorni. Aggiunti i tre toggle di consenso (`recordVisits`/`showVisitors`/`notifyVisits`) al form `/settings`. Nuova pagina `/notifiche` con segna-tutte-come-lette; nav "Notifiche" abilitata. Esteso `scripts/cleanup.ts` per la conservazione dati: purga visite oltre 30 giorni e notifiche lette oltre 90 giorni (le non lette non vengono mai purgate per età). 5 nuovi test di integrazione (notifiche su richiesta/accettazione/like/commento, nessuna auto-notifica, consenso e deduplica delle visite) — `pnpm test:integration` 27 verdi, `pnpm lint`/`pnpm typecheck`/`pnpm test` (20) verdi.
- 2026-09-10 — Claude: **verificato in produzione end-to-end**: creato un post pubblico, like (contatore a 1), commento aggiunto e poi eliminato, post eliminato — feed torna correttamente vuoto. Notato un singolo `POST /home → 503` transitorio durante il primo tentativo di like (probabile cold-start della connessione Neon, solo warning SSL benigno nei log, nessun errore applicativo); i tentativi successivi sono andati a buon fine senza intervento. Non richiede fix.
- 2026-09-10 — Claude: Milestone 4 avviata (post/like/commenti/feed). Nuovo modulo `src/features/posts/` con `createPost`/`deletePost` (solo proprietario), `toggleLike` (idempotente), `addComment`/`deleteComment` (solo autore), tutti verificano la visibilità del post riusando `canReadProfile` di `profiles/policy.ts` (stessa semantica pubblico/amici/privato/bloccati già validata per i profili). `getFeed` in `queries.ts` mostra post propri + pubblici + "solo amici" degli amici, esclude autori bloccati in entrambe le direzioni, paginato per cursore su id. Home page: composer + feed reale al posto del placeholder statico. Immagini nei post non incluse in questo passaggio (richiederebbero legare l'upload lato client al post prima ancora che esista una riga `Post`): segnato come da fare in README. 4 nuovi test di integrazione (regole di visibilità multiple, like idempotente, ownership di commenti/eliminazioni, blocco che nasconde il feed) — `pnpm test:integration` 24 verdi, `pnpm lint`/`pnpm typecheck`/`pnpm test` (20) verdi.
- 2026-09-10 — Claude: **verifica manuale in produzione ha trovato un bug reale**: due account di test (`marco_test`/`andrea_test`), richiesta inviata e poi un doppio click ravvicinato su "Accetta" nella pagina `/amici` faceva sparire la richiesta senza creare l'amicizia. Aggiunto logging diagnostico temporaneo (poi rimosso), riprodotto con un solo click pulito (funzionava) per isolare la causa al doppio-submit concorrente. **Causa**: `respondToFriendRequest` e il ramo di auto-accettazione in `sendFriendRequest` leggevano lo stato `PENDING` e lo aggiornavano in due passaggi separati (`findUnique` + `update`), lasciando una finestra di race in cui un secondo submit concorrente arrivava a tentare una seconda `friendship.create` e falliva silenziosamente (errore ignorato di proposito per gestire "richiesta già risolta"). **Fix**: sostituito con un `updateMany` atomico condizionato su `status: "PENDING"` — un submit duplicato concorrente trova 0 righe aggiornate e non tenta mai la creazione dell'amicizia. Aggiunto test di integrazione che riproduce il doppio accept concorrente sulla stessa richiesta (eseguito 3 volte di fila senza flakiness). Verificato di nuovo in produzione dopo il fix con un click singolo: funziona (`I tuoi amici: Marco Test`). Il doppio-click non è stato ri-testato in produzione dopo il fix (solo in locale via test di integrazione) — se vuoi una conferma anche lì, fammelo sapere.
- 2026-09-10 — Claude: **Milestone 6 completata** (messaggi 1:1). Nuovo modulo `src/features/messages/`: `sendMessage` (crea conversazione + entrambi i `ConversationParticipant` in una transazione se non esiste già, rispetta `messagePermission` del destinatario — FRIENDS/EVERYONE/NOBODY, verifica blocchi, idempotente per `clientId` grazie al vincolo unico esistente su `(conversationId, senderId, clientId)`), `markConversationRead`. Aggiunto `messagePermission` alle impostazioni profilo (default FRIENDS, come lo schema DB). Nuove pagine `/messaggi` (elenco conversazioni con anteprima e badge non letti) e `/messaggi/[username]` (thread con composer e polling client-side ogni 5s — nessun websocket, coerente con la scala di una chat 1:1); pulsante "Manda un messaggio" sul profilo pubblico; nav "Messaggi" abilitata. 2 nuovi test di integrazione (permesso FRIENDS di default + idempotenza clientId + conteggio non letti; permesso EVERYONE/NOBODY e blocco) — `pnpm test:integration` 29 verdi, `pnpm lint`/`pnpm typecheck`/`pnpm test` (20) verdi.
- 2026-09-10 — Claude: **verificato in produzione end-to-end**: `andrea_test` invia un messaggio a `marco_test` (già amici), il thread lo mostra correttamente allineato a destra; `marco_test` vede la conversazione in `/messaggi` con badge "1" non letto e anteprima; aprendo il thread il badge sparisce (mark-as-read confermato). Nessun problema riscontrato.
- 2026-09-10 — Claude: **investigato il bug segnalato dal proprietario** "Vercel Blob: Failed to retrieve the client token" durante l'upload avatar/copertina. Nei log Vercel risultavano tre `POST /api/media/upload 400` (10:42, 11:18, 11:19) su due deploy precedenti a quello attuale, senza dettaglio dell'errore reale (solo il warning SSL benigno). Aggiunto logging diagnostico temporaneo in `route.ts`, deployato (`977f335`), poi riprodotto l'upload in produzione simulando la selezione file via DOM: **entrambe le chiamate sono tornate `200`**. Rimosso il logging (commit `c42c10e`) pensando il bug fosse un fantasma — **errore**: il proprietario lo ha riprodotto di nuovo pochi minuti dopo con una foto reale, e senza il logging il messaggio non era catturabile. Rimesso subito il logging (commit `38db6bd`), il proprietario ha riprovato e **causa trovata**: `ZodError` su `width` (>8000px) — la foto reale caricata (alta risoluzione da smartphone) superava il limite di sanità impostato in `clientMetaSchema`, che era troppo basso per foto moderne. Il messaggio generico "Failed to retrieve the client token" lo genera `@vercel/blob/client` per qualunque risposta non-2xx di `onBeforeGenerateToken`, quindi non indicava la causa specifica finché non si è loggato l'errore reale lato server. **Fix**: alzato il limite `width`/`height` da 8000 a 20000px (commit `d5f2f1a`, rimosso di nuovo il logging diagnostico). Il vincolo che conta davvero resta la dimensione file (5MB). Verificato in produzione con una foto reale: upload riuscito.
- 2026-09-10 — Claude: il proprietario segnala un secondo problema, non un errore ma una lacuna UX: dopo un upload riuscito non c'era alcun riscontro (nessuna anteprima, nessun messaggio), quindi non era chiaro se l'immagine fosse stata effettivamente impostata. **Fix**: `MediaUploader` ora mostra l'immagine corrente (da `profile.avatarUrl`/`coverUrl`) o, appena disponibile, l'anteprima locale del file appena caricato via `URL.createObjectURL`, più un messaggio "Immagine caricata." di conferma (commit `8be6f00`). Verificato in produzione: anteprima aggiornata istantaneamente al colore del file di test caricato, messaggio di conferma visibile. `pnpm lint`/`pnpm typecheck`/`pnpm test` (20) verdi.
- 2026-09-10 — Claude: **Milestone 7 completata** (hardening, suite E2E, misure query, backup/ripristino, deploy). Verificato che header di sicurezza (CSP, X-Frame-Options, Referrer-Policy, Permissions-Policy) fossero già presenti in `next.config.ts` da milestone precedenti: nessuna modifica necessaria. Aggiunta `pnpm test:e2e`: `scripts/e2e.ts` (stesso pattern di `test:integration`: PostgreSQL temporaneo via `embedded-postgres`) fa build+start dell'app e lancia Playwright/Chromium contro il percorso critico (registrazione, login, modifica profilo, post, like, logout, nuovo login). Aggiunto anche un `aria-label` mancante sul pulsante like (prima era solo un numero, senza nome accessibile). Documentate in README le "Misure delle query" (indici compositi già presenti su tutte le query calde; nessun indice aggiuntivo speculativo con i volumi attuali) e "Backup e ripristino" (PITR nativo di Neon, nessun job di backup applicativo custom). Deploy già coperto: l'app è in produzione su Vercel dalle milestone precedenti.
  - **Nota tecnica su un problema di harness incontrato durante lo sviluppo della suite E2E**: asserire la navigazione della pagina subito dopo il click sul pulsante logout risultava intermittentemente instabile in questo sandbox (a volte `chrome-error://chromewebdata/`, a volte la sessione restava attiva) — riprodotto in modo identico anche isolando il test al solo login+logout, sia contro `next dev` che contro un build di produzione (`next start`) locale. **Verificato a mano contro il deploy reale su Vercel che il logout funziona correttamente** (redirect pulito a `/login`), quindi non è un bug applicativo ma una particolarità della rete di loopback locale in questo ambiente. La suite verifica l'effetto reale (cookie di sessione rimosso via `page.context().cookies()`) invece di dipendere dal caricamento della pagina successiva.
  - `pnpm lint`, `pnpm typecheck`, `pnpm test` (20), `pnpm test:integration` (29), `pnpm test:e2e` (1) verdi.
- 2026-09-10 — Claude: **verifica email implementata** (primo item della lista "prima della beta pubblica" in README, dopo che la Milestone 7 ha chiuso la roadmap numerata). Riusato il modello `VerificationToken` già presente nello schema (predisposto per Auth.js, mai usato finora): `sendVerificationEmail` genera un token da 32 byte, lo sostituisce a ogni richiesta (nessun accumulo), scade in 24h; `verifyEmailToken` lo consuma con un `delete` atomico sulla chiave unica (stesso pattern di `resetPassword`: un secondo tentativo concorrente trova la riga già cancellata e fallisce). `registerAccount` invia l'email automaticamente ma non blocca la registrazione se l'invio fallisce (l'account resta utilizzabile, si può reinviare dalle impostazioni). Nuova pagina `/verify-email?token=…` con conferma esplicita via submit (mai una mutazione su semplice GET). Aggiunto `ResendVerificationButton` e stato "Email verificata/non verificata" in `/settings`. 3 nuovi test di integrazione (consumo concorrente, token scaduto, sostituzione del token pendente) — `pnpm test:integration` 32 verdi, `pnpm lint`/`pnpm typecheck`/`pnpm test` (20) verdi.
  - **Blocco operativo scoperto durante il collaudo**: verificato via `vercel env ls production` che in produzione è configurato `MAIL_TRANSPORT` ma **manca `SMTP_URL`** — quindi l'invio email (verifica **e** anche il recupero password preesistente, che condivide lo stesso requisito) fallisce silenziosamente lato server in produzione finché non si imposta un provider SMTP reale. Non è una regressione di questa modifica: era già un blocco elencato in README ("configurazione SMTP/storage") prima di questa sessione. Documentato in README con una nota operativa esplicita.
  - Nota minore sull'harness locale: `pnpm test:e2e` (build+start locale) genera un `ConfigurationError` non fatale durante l'invio dell'email di verifica in registrazione, perché in questo ambiente `.env.local` (un export segnaposto Vercel, `[SENSITIVE]`) viene letto da `next build`/`next start` che forzano `NODE_ENV=production` indipendentemente dall'env passato allo script. Il `try/catch` intorno all'invio (pensato apposta per non bloccare mai la registrazione) assorbe il problema correttamente: il test passa comunque. Non richiede fix, è coerente con l'avviso già presente in README sui file `.env` esportati da Vercel.
